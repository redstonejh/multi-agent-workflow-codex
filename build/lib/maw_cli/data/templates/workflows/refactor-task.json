{
  "id": "refactor-task",
  "name": "Refactor Task",
  "description": "Change internal structure while preserving external behavior.",
  "caps": {"max_agents": 5, "max_parallel": 3},
  "agents": ["conductor", "planner", "worker", "critic", "acceptance_gate"],
  "handoffs": [
    {"from": "conductor", "to": "planner"},
    {"from": "planner", "to": "worker"},
    {"from": "worker", "to": "critic"},
    {"from": "critic", "to": "acceptance_gate"}
  ],
  "required_artifacts": [
    "artifacts/refactor-plan.md",
    "artifacts/behavior-baseline.json",
    "artifacts/behavior-diff.json",
    "artifacts/test-result.json",
    "artifacts/critic-review.md",
    "artifacts/acceptance-result.json"
  ],
  "acceptance_gates": ["behavior-baseline-pre-edit", "behavior-diff-clean", "tests-pass", "scope-contained", "handoffs-valid"],
  "deterministic_checks": [
    {"name": "behavior-baseline", "command": "py maw-tools/behavior_baseline.py capture --manifest <behavior-manifest.json> --output artifacts/behavior-baseline.json"},
    {"name": "behavior-diff", "command": "py maw-tools/behavior_baseline.py verify --manifest <behavior-manifest.json> --baseline artifacts/behavior-baseline.json --output artifacts/behavior-diff.json"},
    {"name": "baseline-tests", "command": "py -m pytest -q"},
    {"name": "handoffs", "command": "py maw-tools/validate_handoffs.py <run_dir>"}
  ]
}
