# Follow-ups

The MVP implements deterministic checks and template activation for the requested advanced agents. Future improvements:

- Add project-specific dependency-map extractors for Python imports instead of validating supplied graph JSON only.
- Add richer calibration checks such as reliability curve bins and classwise calibration summaries.
- Add run-template schema fields for `mode` and `advanced_agents` to make UI filtering easier.
- Add richer example run folders for each advanced template, not only concise usage examples.
